Local SVG icons, if this site ever needs one that Lucide doesn't have.

`astro-icon` scans this directory and warns on every build when it's missing,
so it stays in the tree even while empty. Anything dropped here is available as
`<Icon name="<filename>" />`; the Lucide set is `<Icon name="lucide:…" />`.
